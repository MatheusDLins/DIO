curso = "Curso de Python"
nome_curso = curso
saldo, limite = 200, 200

print(f"curso is nome_curso ? {curso is nome_curso}")

print(f"curso is not nome_curso ? {curso is not nome_curso}")

print(f"saldo is limite ? {saldo is limite}")